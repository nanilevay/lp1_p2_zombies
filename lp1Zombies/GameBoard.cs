using System;

namespace lp1Zombies
{
    /// <summary>
    /// this class will serve as the board for the main game, the rendering 
    /// will depend on player input
    /// </summary>
    class GameBoard
    {
        static GameBoard()
        {

        }

        static void Render()
        {
            // for cycles to render the board
        }
    }
}
