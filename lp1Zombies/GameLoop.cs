namespace lp1Zombies
{
    /// <summary>
    /// this class will setup a game for the program to run
    /// </summary>
    class GameLoop
    {
      // do while game true

        // player input 7 args: x and y dimensions, zombie number, human number
        // player zombies number, human zombies number
        // max turn number
        //e.g. dotnet run -- -x 20 -y 20 -z 10 -h 30 -Z 1 -H 2 -t 1000
    }
}
